<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warna -->
    <solid android:color="@color/mainCyan" />

    <!-- Sudut melengkung -->
    <corners
        android:topLeftRadius="36dp"
        android:topRightRadius="36dp"/>

    <!-- (Opsional) Tambahkan padding -->
    <padding
        android:left="16dp"
        android:top="8dp"
        android:right="16dp"
        android:bottom="8dp" />

</shape>
